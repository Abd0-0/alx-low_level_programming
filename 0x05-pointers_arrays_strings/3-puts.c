#include <stdio.h>
#include "main.h"

/**
 * _puts - a function that prints a string,
 *  followed by a new line, to stdout
 *
 *  @str: a pointer to a string.
 */

void _puts(char *str)
{
	printf("%s\n", str[]);
}
